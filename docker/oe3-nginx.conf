# OE3 nginx config — SPA fallback, no caching for config.js
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # config.js must not be cached — it contains runtime config
    location = /config.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        try_files $uri =404;
    }

    # SPA fallback: all routes serve index.html
    # index.html must revalidate — it references hashed assets; caching it
    # would pin browsers to an old bundle for up to an hour after deploys.
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, must-revalidate" always;
    }

    # Static assets with long cache
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        try_files $uri =404;
    }
}
